package fajnelokalne.demo.specifications;

import fajnelokalne.demo.entity.City;
import org.springframework.data.jpa.domain.Specification;
import org.springframework.stereotype.Service;

import java.util.Locale;

@Service
public class CategorySpecifications {

//    public Specification<Category> hasUsernameLike(String username) {
//        return (root, query, builder) -> builder.like(root.get("username"), "%" + username + "%");
//    }

}
